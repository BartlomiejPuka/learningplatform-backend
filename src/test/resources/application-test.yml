spring:
  datasource:
    url: jdbc:h2:mem:learning_platform;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=false;MODE=MYSQL
    driverClassName: org.h2.Driver
    username: sa
    password:
    data:
      classpath:test-users.sql,
      classpath:data-course-categories.sql,
      classpath:data-courses.sql,
      classpath:data-lessons.sql,
      classpath:data-tasks.sql,
      classpath:view-course-products.sql

